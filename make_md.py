import os

content = r'''# 采购系统需求文档

## 一、项目背景与目标

本系统用于企业内部采购流程管理，解决采购申请、审批、订单跟踪、供应商管理分散的问题。目标是将采购全流程线上化，实现申请可追溯、审批可监控、订单可查询。

## 二、技术栈与约束

- 后端语言：Python 3.11
- Web 框架：FastAPI
- 数据库：SQLite（本地开发），后续可切换 PostgreSQL
- 前端：HTML + CSS + 原生 JavaScript（单页面，不引入前端框架）
- 运行环境：Windows 10/11
- 所有密码必须用 bcrypt 哈希存储，禁止明文
- 所有金额字段用 Decimal 类型，禁止用 float

## 三、功能需求清单

### FR-1 用户管理

- FR-1.1 当用户访问系统时，系统应显示登录页面，要求输入用户名和密码。
- FR-1.2 当用户提交登录信息时，系统应校验用户名是否存在、密码哈希是否匹配。
- FR-1.3 当登录成功时，系统应跳转到采购申请列表页，并在顶部显示当前用户名。
- FR-1.4 当登录失败时，系统应提示"用户名或密码错误"，不透露具体是哪一项错误。
- FR-1.5 系统应支持三种角色：申请人、审批人、管理员。

### FR-2 采购申请

- FR-2.1 当申请人登录后，系统应显示"新建采购申请"按钮。
- FR-2.2 当申请人填写申请时，系统应要求填写：物品名称、数量、预估单价、用途说明。
- FR-2.3 当申请人提交申请时，系统应自动计算预估总价（数量 × 预估单价），并将状态设为"待审批"。
- FR-2.4 当申请人查看申请列表时，系统应显示每条申请的编号、物品名称、总价、状态、提交时间。
- FR-2.5 当申请状态为"待审批"时，申请人应可以撤回申请。

### FR-3 审批流程

- FR-3.1 当审批人登录后，系统应显示所有"待审批"状态的申请。
- FR-3.2 当审批人点击某条申请时，系统应显示完整详情，包括申请人、物品、数量、单价、总价、用途。
- FR-3.3 当审批人点击"通过"时，系统应将状态改为"已通过"，并记录审批人和审批时间。
- FR-3.4 当审批人点击"驳回"时，系统应要求填写驳回理由，并将状态改为"已驳回"。
- FR-3.5 当申请被审批后，系统应向申请人显示审批结果和审批意见。

### FR-4 供应商管理

- FR-4.1 当管理员登录后，系统应显示供应商列表。
- FR-4.2 当管理员新增供应商时，系统应要求填写：供应商名称、联系人、联系电话、地址。
- FR-4.3 当管理员编辑供应商时，系统应允许修改所有字段。
- FR-4.4 当管理员删除供应商时，系统应二次确认，避免误删。
- FR-4.5 当供应商已关联采购订单时，系统应禁止删除，并提示"该供应商存在关联订单"。

### FR-5 订单跟踪

- FR-5.1 当采购申请审批通过后，系统应允许管理员生成采购订单。
- FR-5.2 当订单生成时，系统应记录：订单编号、关联申请、供应商、下单时间、预计到货时间。
- FR-5.3 当管理员更新订单状态时，系统应支持：已下单、已发货、已到货、已验收。
- FR-5.4 当订单状态变更时，系统应记录变更时间和操作人。

## 四、验收标准

- AC-1 执行 `python -m pytest tests/` 时，所有测试通过。
- AC-2 执行 `python main.py` 后，浏览器访问 `http://localhost:8000` 能打开登录页面。
- AC-3 用管理员账号登录后，能完成新增供应商、生成订单的完整流程。
- AC-4 数据库中 users 表的 password_hash 字段，不能出现任何明文密码。
- AC-5 所有金额计算结果显示两位小数。

## 五、文件路径规划

procurement-system/
├── main.py # 应用入口，启动 FastAPI
├── database.py # 数据库连接与初始化
├── models.py # 数据表模型定义
├── auth.py # 登录与权限校验
├── routers/
│ ├── users.py # 用户相关接口
│ ├── requests.py # 采购申请接口
│ ├── approvals.py # 审批接口
│ ├── suppliers.py # 供应商接口
│ └── orders.py # 订单接口
├── static/
│ ├── style.css # 页面样式
│ └── app.js # 前端交互逻辑
├── templates/
│ ├── login.html # 登录页
│ ├── requests.html # 申请列表页
│ ├── approvals.html # 审批页
│ ├── suppliers.html # 供应商管理页
│ └── orders.html # 订单跟踪页
└── tests/
├── test_auth.py # 登录测试
├── test_requests.py # 申请流程测试
└── test_orders.py # 订单流程测试

## 六、开发顺序建议

1. 先完成 database.py 和 models.py，建好所有数据表。
2. 再完成 auth.py 和登录页面，确保能登录。
3. 然后按 FR-2 → FR-3 → FR-4 → FR-5 的顺序逐个实现功能模块。
4. 每个模块完成后，让 Cursor 运行对应测试，通过后再进入下一个模块。

## 七、给 Cursor 的使用提示

- 在 Plan 模式下输入："请阅读 @采购系统需求.md，按第六节的开发顺序，先生成 database.py 和 models.py 的完整代码。"
- 每完成一个模块，用 `@采购系统需求.md` 引用本文档，让 Agent 对照验收标准检查。
- 遇到报错时，把报错信息和本文档一起发给 Agent，让它对照需求修复。
'''

# 获取桌面路径（兼容 Windows 和 macOS）
desktop = os.path.join(os.path.expanduser("~"), "Desktop")
if not os.path.exists(desktop):
    desktop = os.path.join(os.path.expanduser("~"), "桌面")

filepath = os.path.join(desktop, "采购系统需求.md")

with open(filepath, "w", encoding="utf-8") as f:
    f.write(content)

print(f"文件已生成：{filepath}")